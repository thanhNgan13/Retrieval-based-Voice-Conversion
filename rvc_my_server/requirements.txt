# ---- Web framework ----
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0
python-multipart==0.0.12

# ---- Auth ----
PyJWT==2.9.0
bcrypt==4.2.0

# ---- Firebase / GCP ----
firebase-admin==6.5.0
google-cloud-firestore==2.19.0
google-cloud-storage==2.18.2

# ---- Utilities ----
python-dotenv==1.0.1
requests>=2.32.0
celery[redis]==5.4.0
redis==5.2.0
# setuptools provides pkg_resources, which fairseq 0.12.2 still imports.
# Pin <81 because pkg_resources is deprecated in newer setuptools and may be removed.
setuptools<81

# ----------------------------------------------------------------
# RVC inference dependencies (copied/aligned with rvc_standalone)
# ----------------------------------------------------------------
# IMPORTANT: pip < 24.1 required for fairseq 0.12.2 (omegaconf metadata).
# Run BEFORE installing: python -m pip install -U "pip>=23.2,<24.1"
# ----------------------------------------------------------------

# Numerical / audio core
numpy==1.23.5
scipy
numba==0.56.4
llvmlite==0.39.0
librosa==0.9.1
soundfile>=0.12.1
audioread
resampy>=0.4.2
pydub>=0.25.1
pedalboard>=0.9.16
ffmpeg-python>=0.2.0
ffmpy==0.3.1
av

# RVC model core
# NOTE: torch / torchaudio / torchvision are installed SEPARATELY by `install_deps.py`
#       which picks the right CUDA wheel (cu118 / cu121 / cu124) based on the local
#       NVIDIA driver. If you bypass install_deps.py and run `pip install -r requirements.txt`
#       directly, you must install torch yourself FIRST — otherwise fairseq below will
#       pull a CPU-only torch from PyPI which silently disables GPU inference.
fairseq==0.12.2
faiss-cpu==1.7.3

# F0 extractors
praat-parselmouth>=0.4.2
pyworld==0.3.2
torchcrepe==0.0.20
torchfcpe

# Inference helpers
joblib>=1.1.0
Cython
tqdm>=4.63.1
matplotlib>=3.7.0
matplotlib-inline>=0.1.3
Pillow>=9.1.1
scikit-learn

# ONNX runtimes (RVC checkpoint export support is optional)
onnxruntime; sys_platform == 'darwin'
onnxruntime-gpu; sys_platform != 'darwin'

# i18n / misc (transitive from RVC modules)
Jinja2>=3.1.2
PyYAML>=6.0
tensorboardX
tensorboard
fsspec>=2022.11.0
omegaconf<2.1
